// import { Link, useLocation } from 'react-router-dom';
// import PropTypes from 'prop-types';
// // import css from './List.module.css'

// export default function MovieInformation(movie) {
//   console.log(movie);
//   const { adult, title, id, vote_average, overview, genres} = movie;
// //   const location = useLocation();
//   console.log(({adult}));
//   return (
//     <>
//     <h1>Title:{title}</h1>
    
//       {/* {films.map(({ id, name, title }) => (
//         <Link key={id} to={`/movies/${id}`} state={{ from: location }}>
//           {name ? (
//             <li className={css.item}>{name}</li>
//           ) : (
//             <li className={css.item}>{title}</li>
//           )}
//         </Link> */}
//       {/* ))} */}
    
//     </>
//   );
// }

// // ListOnSearch.propTypes = {
// //   films: PropTypes.arrayOf(PropTypes.object),
// // };